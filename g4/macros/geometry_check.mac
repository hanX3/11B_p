/run/initialize
/control/verbose 1
# Print the Si array configuration (strip counts, forward-ring enable/distance).
# The Geant4 overlap check runs at construction; the printout above must show
# Si_Barrel / Si_BackwardAnnular / Si_ForwardAnnular placed with no overlaps.
/si/printConfig
/run/beamOn 1
